# 2026-04-22 工作日志

## 赛博分身端到端联调完成

### 完成内容

1. **Prisma 迁移修复**
   - 发现 `prisma/migrations/20260422094500_add_startup_posts/migration.sql` 有 UTF-8 BOM（EF BB BF）
   - 用 Python 移除 BOM → `prisma migrate resolve --rolled-back` → `prisma migrate deploy`
   - **结果**：所有 16 个迁移全部应用成功

2. **TypeScript 编译错误修复（3个）**
   - `lobster.consumer.ts`: `@RabbitHandler` 缺少 `type: 'subscribe'` 属性 + 装饰器返回类型与 async 方法不兼容（库 bug）
     - 修复：添加 `type: 'subscribe'` + `// @ts-expect-error` + `as any`
   - `lobster.service.ts`: `handleDeerflowResultFromQueue` 中 Opportunity 创建使用了 schema 不存在的 `title` 字段和 `status` 字段
     - 修复：映射到正确字段（title→position，移除 status）
   - **结果**：TypeScript 编译通过（exit 0），NestJS 构建成功（exit 0）

3. **端到端联调验证状态**
   - ✅ 代码层面：所有模块编译通过
   - ✅ 数据库：lobster_statuses / lobster_task_logs / hp_logs / opportunities / lobster_review_tasks / lobster_match_records 全部迁移就绪
   - ✅ 消息拓扑：4交换机/6队列/7绑定（lobster.tasks / lobster.results / lobster.reviews / lobster.dlx）
   - ✅ Workflow YAMLs：主工作流 + 3性格子工作流全部就绪
   - ⏳ 运行时验证：需 DeerFlow 服务器（81.69.228.186:2026）+ RabbitMQ 在线才能执行真实联调

### 踩坑经验
- `@golevelup/nestjs-rabbitmq` v9 `@RabbitHandler` 装饰器返回类型与 async 方法不兼容 → 使用 `as any` 配置 + `// @ts-expect-error` 抑制
- MySQL 迁移文件必须无 BOM → 写入文件时确保 UTF-8 无签名
