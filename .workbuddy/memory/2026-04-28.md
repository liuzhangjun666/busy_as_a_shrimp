# 2026-04-28 工作日志

## DeerFlow docker-compose.yaml 修复
- 将 langgraph 服务从 `build:` 模式改为 `image: deer-flow-langgraph:fixed2`
- 修改文件：`/home/ubuntu/deer-flow/docker/docker-compose.yaml`
- 状态：✅ 修改成功

## DeerFlow .env 文件创建
- 创建路径：`/home/ubuntu/deer-flow/docker/.env`
- 关键变量：DEER_FLOW_HOME, DEER_FLOW_REPO_ROOT, DEER_FLOW_CONFIG_PATH, DEER_FLOW_EXTENSIONS_CONFIG_PATH, DEER_FLOW_DOCKER_SOCKET, BETTER_AUTH_SECRET
- 配置文件实际位置：项目根目录 `/home/ubuntu/deer-flow/`（不是 backend/）

## DeerFlow 全栈重启成功
- 4个服务全部运行：nginx(2026), gateway(8001), frontend(3000), langgraph(2024)
- 镜像：deer-flow-langgraph:fixed2
- langgraph: 10 workers 可用，active=0 完全空闲
- 残留容器清理：使用 `docker ps -a --format` + grep + xargs 批量删除
